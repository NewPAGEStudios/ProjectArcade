using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemySpawnPoint : MonoBehaviour
{
    public Transform min;
    public Transform max;
    //private void OnDrawGizmos()
    //{
    //    Gizmos.color = Color.red;
    //    Gizmos.DrawWireCube(new Vector3(min.position.x + (max.position.x - min.position.x) / 2, transform.position.y, min.position.z + (max.position.z - min.position.z) / 2), new Vector3(max.position.x - min.position.x, 0.1f, max.position.z - min.position.z));
    //}
}
